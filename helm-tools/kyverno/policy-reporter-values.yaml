policyReporter:
  # Cấu hình UI nếu bạn muốn xem giao diện Web của Policy Reporter (Tùy chọn, tắt đi cho nhẹ RAM)
  ui:
    enabled: false

  # CẤU HÌNH BẮN DISCORD CHUẨN CHỈNH
  channels:
    - name: "discord-alerts"
      type: discord
      # Dán chính xác link Webhook Discord của bạn vào đây
      webhook: "https://discord.com/api/webhooks/xxxxxxxxxxxxxxxxxxxxxxxx"
      minimumPriority: "warning"
 
global:
  watchNamespace: "" # Quét toàn bộ cluster một cách chủ động
